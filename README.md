# Ukraine Victory Index

Хобі-проєкт: щотижнева оцінка балансу сил у війні росії проти України на основі
відкритих даних. Не військова аналітика — сумлінний аматорський естімейт, цінність
якого в **послідовній динаміці**, а не в абсолютних цифрах.

## Два індекси

| Індекс | Шкала | Семантика |
|--------|-------|-----------|
| **Баланс сил** | −100…+100 | Поточне співвідношення спроможностей. 0 = паритет (відносно асиметричних цілей сторін), плюс = перевага України. |
| **Стратегічний** | 0–100 | Наближення до «стійкого вигідного миру»: гарантії безпеки (40%), ЄС та відбудова (30%), справедливість (30%). 0 = поразка, 100 = мир досягнуто. |

Індекси мають різну семантику і ніколи не усереднюються між собою.

## Структура

- `.claude/skills/victory-index/` — скіл, що збирає дані, рахує індекси і пише звіти
  - `SKILL.md` — процедура запуску
  - `references/methodology.md` — рубрики оцінювання, ваги, формули
  - `references/sources.md` — каталог джерел даних
  - `references/report-template.md` — шаблон звіту
- `journal/` — тижневі звіти (`YYYY-MM-DD.md`)
- `data/history.json` — машиночитна історія індексів (для графіків)

## Використання

У Claude Code в цьому репозиторії: `/victory-index` або просто «порахуй індекс».
Скіл сам збере свіжі дані, оцінить фактори відносно попереднього запуску, запише
звіт у журнал і оновить історію.

## Автоматизація

Щосуботи о **06:00 UTC** (09:00 Києва влітку, 08:00 взимку) Routine
«Victory Index — щотижневий розрахунок» будить свіжу сесію Claude Code, яка
виконує скіл `victory-index` від гілки `main`: збирає дані, рахує індекси, пише
звіт у `journal/`, оновлює `data/history.json`, комітить і пушить у `main`.
Коли звіт готовий — приходить пуш-сповіщення на телефон.

Керування (інструменти MCP-сервера Claude Code Remote, у будь-якій сесії Claude Code):

| Дія | Виклик |
|-----|--------|
| Подивитись усі Routine і їх ID | `list_triggers` |
| Запустити позапланово, не чекаючи суботи | `fire_trigger` з `trigger_id` |
| Поставити на паузу | `update_trigger` з `enabled: false` |
| Увімкнути знову | `update_trigger` з `enabled: true` |
| Змінити час/розклад | `update_trigger` з новим `cron_expression` (UTC) |
| Змінити текст завдання | `update_trigger` з новим `prompt` |
| Видалити | `delete_trigger` з `trigger_id` |

Простіше за все — попросити словами: «покажи мої Routine», «постав щотижневий
індекс на паузу», «запусти індекс зараз». Розклад також видно й редаговано в
розділі Routines на claude.ai.

## Етапи

- [x] Етап 1: скіл розрахунку + журнал звітів
- [x] Автоматичний щотижневий запуск — див. розділ «Автоматизація»
- [ ] Етап 2: HTML-дашборд з індексами та графіками — див. [PLAN.md](PLAN.md), Таск 2
